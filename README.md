# jenkins-running

# Run this command on killercoda and direclty jenkins will be installed . 

```bash
curl -fsSL https://raw.githubusercontent.com/prasadkelkar2003/jenkins-running/main/jenkins-run.sh | bash
```

after this just access the port 8080

```bash
sudo git config --system 'http.https://github.com.ipResolve' ipv4
```
the above command if there is any issue like can't connect to github
